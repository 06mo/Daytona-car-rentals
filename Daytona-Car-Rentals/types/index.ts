export type NavItem = {
  href: string;
  label: string;
};

export type ButtonVariant = "primary" | "secondary" | "ghost" | "danger" | "outline";
export type ButtonSize = "sm" | "md" | "lg";

export type { TuroVehicle, VehicleCategory } from "@/lib/data/vehicles";
